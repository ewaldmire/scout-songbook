\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{fancyhdr}

% Page layout
\geometry{letterpaper, top=.75in, bottom=.75in, left=1in, right=1in}

% Fancyhdr: remove all rules, only page number at bottom
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Non-serif (sans-serif) font
\renewcommand{\familydefault}{\sfdefault}

% Make all text bold + larger
\AtBeginDocument{\bfseries\large}

% Define \songtitle if not defined
\providecommand{\songtitle}[1]{\section*{#1}}

\ifdefined\songfile\else
  \def\songfile{}
\fi

\begin{document}

\input{\songfile}

\end{document}
